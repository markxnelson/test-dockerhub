box: ubuntu

build:
  steps:
    - script:
      code: | 
        echo "do something"
    - internal/docker-push:
      username: $REPO_USERNAME
      password: $REPO_PASSWORD
      repository: https://hub.docker.io/v2
      registry: markxnelson/test-dockerhub
      tag: latest
