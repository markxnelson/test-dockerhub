box: ubuntu

build:
  steps:
    - script:
      code: | 
        echo "do something"
    - internal/docker-push:
      username: $REPO_USERNAME
      password: $REPO_PASSWORD
      registry: https://hub.docker.io/v2
      repository: markxnelson/test-dockerhub
      tag: latest
